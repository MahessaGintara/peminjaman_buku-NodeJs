extends auth_base.pug 

block content
    body.hold-transition.register-page 
        .register-box 
            .register-logo
                a(href='#')
                    b
                        | Perpustakaan
            .register-box-body
                p.register-box-msg
                    | Register a new membership
                form(action="/register",method="post")
                    - if (messages.msg_error) 
                        .alert.alert-danger.alert-dismissable 
                            button.close(type='button', data-dismiss='alert', aria-hidden='true') 
                                | ×
                            h4
                                i.icon.fa.fa-ban
                                | Alert! 
                            | !{messages.msg_error}
                    .form-group.has-feedback
                        input(type="text",placeholder="Username",name="txtName", value=`${name}`).form-control
                        span.glyphicon.glyphicon-user.form-control-feedback
                    .form-group.has-feedback
                        input(type="email",placeholder="Email",name="txtEmail", value=`${email}`).form-control
                        span.glyphicon.glyphicon-envelope.form-control-feedback
                    .form-group.has-feedback
                        input(type="password",placeholder="Password",name="txtPassword",value=`${password}`).form-control
                        span.glyphicon.glyphicon-lock.form-control-feedback
                    .form-group.has-feedback
                        input(type="password",placeholder="Password Confirmation",name="txtPasswordConfirmation", value=`${passwordConfirmation}`).form-control
                        span.glyphicon.glyphicon-lock.form-control-feedback
                    .row
                        .col-xs-8
                            .checkbox 
                                    label
                                        input(type="checkbox", name='boolRemember', value=1)
                                        |   Remember Me
                        .col-xs-4
                            button(type="submit").btn.btn-primary.btn-block.btn-flat
                                | Register
                a(href='/login').text-center
                    |	I already have a membership